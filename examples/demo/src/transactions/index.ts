import TransactionsIcon from '@material-ui/icons/Send';

import TransactionsList from './TransactionsList';

const resource = {
    list: TransactionsList,
    icon: TransactionsIcon,
};

export default resource;
